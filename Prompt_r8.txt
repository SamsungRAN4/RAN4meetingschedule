Act as an expert Data Analyst and Frontend Developer.

General explanation for the work (separated into three Tasks below): 
 Task-1: Analyze two data sources (1. 3GPP RAN4 meeting schedule information (provided by pptx file), 2. Samsung RAN4 team's internal responsibility information (provided by spreadsheet file(s)))
 Task-2: Use the outcome from Task-1, to find Samsung delegate who is responsible for each topics in the meeting schedule, and add that information to the meeting schedule information. 
 Task-3: Use the outcome from Task-2, to generate a single-file interactive HTML dashboard (which provides the html codes).

Most important rule:
 All the relevant information generated to the html file should be based on these files provided (CRITICAL). 

### Task-1
 In Task-1: How to use the attached pptx file for 3GPP RAN4 meeting schedule information: 
 1. The PPTX file containing the official 3GPP RAN4 meeting schedule. 
 2. For a certain workday (from Monday to Friday), one table is provided in one page. Usually there are five tables corresponding to 5 workdays. 
 3. For one table, the table row means "timeslot" and the table column means "4 physical meeting rooms" (e.g., called as RAN4 Main (Yang Session), RAN4 Breakout 1 (BR1) (Shan Session), RAN4 Breakout 2 (BR2)(Gene Session), Ad-hoc room). For one cell in this table, the corresponding column and row designate the time and location for the topic(s) provided in this cell. 
 4. In every cell, the information provided in this cell may contain (1) explanation information, e.g., "Rel-20 RRM", "NWM"; (2) topic information (e.g, "[202] NR_UAV_req_RRM". The topic information usually starts with topic index, e.g., [202] in this case. One cell may contain multiple topics.) (3) other information (e.g., "chaired by someone", "(14:30 – 15:30)", etc.) All the information (1) (2) (3) are important and needs to be contained in the output. 
 4a. 
 5. In some cells, there could be some explanation information (such as Rel-20 RRM) which is not the topic name (which should start with topic identifier like [201]), which means the category and all topics under it are in this category. Just keep the explanation information as it is. 
 6. In every cell, the information provided in this cell is critical and don't make any change to this information. The cell's row and column information is also critical, don't merge or move to other row or column. 
 7. In some cells, something information is provided like "6G SI: Sensing (cont)" means the previous topic in this room (in previous timeslot of the same room) will be continued. So in this case, if there is no topic index in that cell, it means the previous topic index will be continued.
 8. The timeslot and room information for every cell is only decided by the row and column of the table. (CRITICAL) Even some cell in the column of "Ad-Hoc" contains the information like "Breakout-1 Ad-Hoc" or "BR-1 ad-hoc", it doesn't means the room is changed to Breakout-1 Room. Again, the timeslot and room information for every cell is only decided by the row and column of the table. 
 9. In some cells, the explanation information like "BR1 Ad-hoc: xxx" is provided, this should not be interpreted as "the location of this topic is BR1 room". the timeslot and room information for every cell is only decided by the row and column of the table. 


In Task-1: How to use the attached spreadsheet file(s) (excel file or cvs files): 
 1. The spreadsheet file(s) file containing our internal work splitting, mapping topic index (provided in the column called "#" and/or in the column called "Topic title". e.g., "[118][101-A] 6G system parameter (part I)" means the topic index is [101-A], while [118] is the meeting name) to specific Samsung delegate(s) (e.g., Jackson, etc.) provided in the column called "Samsung Major Responsibility".
 2. In the spreadsheet file(s), one topic (with a specific topic index) may have the work level as "monitoring". This information is useful and could be used in Task-2 and Task-3. 
 3. In the spreadsheet file(s), one topic (with a specific topic index) may have multiple Samsung delegates to cover, e.g., [101-A] has "Taekhoon / Jackson", so it means both Taekhoon and Jackson needs to follow it. Some topics has many Samsung delegates. e.g., if there are multiple delegates (like "Jackson / Taekhoon / Lili", then consider all three delegates as internal owners). All mentioned delegates needs to be considered for this topic. Don't remove any one but just follow what the spreadsheet file(s) says. (CRITICAL)
 4. Current Samsung delegates are: Jackson, Tina, Bozhi, Taekhoon, Xin, Lili, Yanze, Dong, Xiaoming, Xinyan and Hongjun. Note: Xinyan and Xin are two delegates.
 
In Task-1: if you can't parse these files successfully, stop this task (don't use history data, but just using the attached files provided to you this time) (CRITICAL)

### Task-2
 1. Use the outcome from two data sources (1. 3GPP RAN4 meeting schedule information (provided by pptx file), 2. Samsung RAN4 team's internal responsibility information (provided by spreadsheet file(s))). 
 2. For the output from the meeting schedule, use the topic index to find out the Samsung delegate(s) who is responsible for that topic and add the corresponding delegate name(s) after every topic. 
 3. For each delegate, identify the possible colliding schedule. Colliding schedule means: one delegate is responsible for topics which are contained in two schedule cells which are arranged in two rooms but in the same timeslot). Because one delegate can't physically be in two rooms, so it cause the colliding schedule. If in one schedule cell (for a timeslot in one room), there are two topics handled by the same person, it is not colliding, because the topics will be handled sequentially. 
 4. If a cell (like "Lunch", "Coffee break", "Opening", or a topic not covered by any member) has no assigned internal delegate, it is okay and just left as "null".
 5. Some of the topic in the schedule may have multiple sub-topic index corresponding to that. e.g., the topic with the topic index of [101] has two topics [101-A] and [101-B] corresponding to that. So when [101] appears in the schedule, the corresponding delegates for both [101-A] and [101-B] should be added. 

### Task-3
 Generate a single-file HTML dashboard using Vanilla JavaScript and Tailwind CSS (via CDN). Do not use React or external frameworks.

**UI Requirements:**
 1. **Tabs:** Create a top navigation bar with tabs for each day of the meeting (Monday - Friday).
 2. **Grid Layout:** Create a responsive table/grid with 5 columns: [Time] | [Main Room (Yang)] | [Breakout Room-1 (Shan)] | [Breakout Room-2 (Gene)] | [Ad-hoc Room]. Allow horizontal scrolling on mobile.
 3. **Uniform Row Heights & Stretching (CRITICAL):** For each time slot (row), make sure the total height of all entries is the SAME across all rooms. Apply `h-full flex flex-col` to the table cells, and use `flex-1` on the individual topic cards inside them. This ensures that if one column has 1 topic and another has 3 topics, the 1 topic stretches to match the height of the 3 topics perfectly. For explanation information like "NTN", "Rel-20 RRM", which don't contain any topic but just some explanation information for the follow-up topics in one cell, don't generate one independent card for this explanation information. 
 4. **Per person highlighted view:** In the top of page, give a selection box for all delegates (e.g., Jackson, Tina, Bozhi, Taekhoon, Xin, Lili, Yanze, Dong, Xiaoming, Xinyan, Hongjun etc.), if selected, the relevant topics for that delegate are highlighted. In the "show all" view (that is the default view), show the conflicting agenda for everyone. If one person is selected, just show conflicting for this person. 
 5. **Full-Row Events:** Items like "Lunch" or "Coffee Break" must span across all 4 room columns.
 6. **Color-Coding & Gray-out Logic (CRITICAL):**
 - If a topic has NO internal owner (`owner: null`), it MUST be visually grayed-out (light gray background, muted text) so the team knows it requires no coverage.
 - If a topic HAS an internal owner, apply a specific color theme based on its room (Main = Blue, Shan = Teal, Gene = Purple, Ad-hoc = Orange).
 - Display the session name clearly and put the owner's name below it with a 👤 icon. 
 7. **Several topics in one cell (timeslot in one room):** put it topic by topic (as separate cards), and mark the responsible person after each topic. If multiple topic identifiers are used together (such as "[208][209] R19 NR_MIMO_Ph5_RRM, Chaired by Yanze Fu (Samsung)"), it means topics are combined and treated together, and usually these topics belong to one topic, so you don't need to go with "topic by topic" manner for this case. 
 8. **Home Support:** mark Xinyan as "Xinyan (Home Support)" and Hongjun as "Hongjun (Home Support)". If one topic is handled by Xinyan or Hongjun with other delegate together, don't need to categorize as home support. 
 9. **Monitoring:** when one topic is marked as "monitoring" in output from the spreadsheet file(s), make the corresponding session's delegate as "name (monitoring)", such as for A-IoT, make as "Tina (monitoring)". And mark the corresponding topic and delegate in grey to lowlight the topic. 
 10. **Content Checking:** "what is shown in every cell of the schedule": on top of the original information provided by pptx (all information (1) explanation information, (2) topic information, (3) other information should be included), just add the corresponding responsible delegates. Pls. don't add other information. For one timeslot has multiple topics, don't miss anything or move topic to other room, strictly follow the original schedule provided in pptx. 
 11. all content in html file should be in English

### Output:
 First, briefly confirm that you have successfully parsed the provided files and mapped the delegates. Then, output ONLY the complete, working HTML code inside a single code block. Ensure the schedule data is embedded as a structured JavaScript object (`const scheduleData = {...}`) within the file so it renders dynamically.